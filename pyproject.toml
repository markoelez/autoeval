[project]
name = "autoeval"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.50.2",
]

[project.scripts]
hello = "autoeval:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
